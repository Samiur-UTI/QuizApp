import React from 'react'

interface Props {
    
}

const Freequiz = (props: Props) => {
    return (
        <div>
            Here we will create a new Freequiz component
        </div>
    )
}

export default Freequiz
